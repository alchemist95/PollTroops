<%= form_for(@poll) do |f| %>
  <% if @poll.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@poll.errors.count, "error") %> prohibited this poll from being saved:</h2>

      <ul>
      <% @poll.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :topic %><br>
    <%= f.text_area :topic, rows: 4, required: true, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :category %><br>
    <%= f.select :category, ["Educational", "Fun", "Corporate", "Spiritual", "Sports"] %>
  </div>


<div class="panel panel-default">
<div class="panel-options">Option</div>
<div class="panel-body">
<%= f.fields_for :vote_options do |x| %>
<%= render 'vote_option_fields', f: x %>
<% end %>

<div class="links">
<%= link_to_add_association 'Add Option', f, :vote_options %>
</div>

</div>
  </div>




  <div class="actions">
   <%= f.submit 'Create', class: 'btn btn-primary btn-lg' %>
  </div>
<% end %>
</div>